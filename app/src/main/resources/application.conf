ktor {
    deployment {
        port = 8080
        host = 0.0.0.0
    }
    deployment {
        port = ${?PORT}
        host = ${?HOST}
    }
    application {
        modules = [ com.shadowconnect.ApplicationKt.module ]
    }
}
